name: "Poseidon Linux Non-Root Base Survey"
description: "Base non-root Linux survey using Poseidon native commands"
trigger: manual
run_as: self

steps:
  # Agent and host context

  - name: "print c2"
    description: "Print Poseidon's active C2 configuration"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: print_c2

  - name: "config"
    description: "Get Poseidon configuration and host information"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: config

  - name: "get user"
    description: "Get the current user context"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: getuser

  - name: "timestamps"
    description: "Collect local time, UTC time, and Unix epoch"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "date; date -u; date +%s"

  - name: "environment"
    description: "Collect process environment variables"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: getenv

  - name: "host identity"
    description: "Collect sessions, cwd, identity, kernel, hostname, and OS release data"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "w; pwd; id; whoami; uname -a; hostname; cat /etc/os-*"

  # Filesystem survey

  - name: "list home"
    description: "Recursively list /home to depth 2"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/home"
        depth: 2

  - name: "triage home"
    description: "Triage potentially interesting files under /home"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: triagedirectory
      params: "/home"

  - name: "list opt"
    description: "List /opt"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/opt"

  - name: "list srv"
    description: "List /srv"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/srv"

  - name: "list filesystem root"
    description: "List the filesystem root"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/"

  - name: "list tmp"
    description: "List /tmp"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/tmp"

  - name: "list shared memory"
    description: "List /dev/shm"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/dev/shm"

  - name: "list var tmp"
    description: "List /var/tmp"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/var/tmp"

  - name: "list var log"
    description: "Recursively list /var/log to depth 2"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/var/log"
        depth: 2

  - name: "list web root"
    description: "Recursively list /var/www to depth 2"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/var/www"
        depth: 2

  - name: "list passwd"
    description: "Get metadata for /etc/passwd"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/etc/passwd"

  - name: "list shadow"
    description: "Get metadata for /etc/shadow without reading it"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ls
      params_dictionary:
        path: "/etc/shadow"

  - name: "login history"
    description: "Collect available login history"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "last -a 2>/dev/null"

  - name: "drives"
    description: "Enumerate mounted drives"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: drives

  - name: "inode usage"
    description: "Collect filesystem inode usage"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "df -i"

  - name: "mounts"
    description: "Collect mounted filesystem details"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "mount"

  - name: "process tree"
    description: "Collect a detailed process tree"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "ps -elf --forest"

  - name: "interfaces"
    description: "Collect network interface information using Poseidon's native command"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: ifconfig

  - name: "diagnostic modules"
    description: "Check loaded kernel modules for diagnostic-related entries"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "cat /proc/modules | grep diag"

  - name: "network sockets"
    description: "Collect listening and established network sockets"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "netstat -antpu || ss -antpu"

  - name: "ip addresses"
    description: "Collect IP address configuration"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "ip -c a || ifconfig -a"

  - name: "routes"
    description: "Collect the routing table"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "ip route || route -n"

  - name: "neighbors"
    description: "Collect the neighbor or ARP table"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "ip neigh || arp -an"

  - name: "recent filesystem changes"
    description: "Find files changed in the last 120 minutes on the root filesystem"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: >-
        find / -xdev -mmin -120 -printf '%M %u %g %s %TY-%Tm-%Td %TH:%TM:%TS %p\n' 2>/dev/null | sort -k6

  - name: "cron locations"
    description: "List common system and user cron locations"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "ls -latr /etc/cron* /etc/cron.* /var/spool/cron /var/spool/cron/crontabs /var/cron"

  - name: "system crontab"
    description: "Read the system crontab"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: cat
      params: "/etc/crontab"

  - name: "ptrace scope"
    description: "Collect the Yama ptrace restriction setting"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: shell
      params: "sysctl kernel.yama.ptrace_scope"

  - name: "download sshd config"
    description: "Download the OpenSSH server configuration when readable"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: download
      params: "/etc/ssh/sshd_config"

  - name: "download passwd"
    description: "Download the local account database"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: download
      params: "/etc/passwd"

  - name: "download fstab"
    description: "Download the static filesystem configuration"
    inputs:
      CALLBACK_ID: env.display_id
    action: task_create
    continue_on_error: true
    action_data:
      callback_display_id: CALLBACK_ID
      command_name: download
      params: "/etc/fstab"
